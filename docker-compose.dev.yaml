services:
  api:
    extends:
      file: docker-compose.base.yaml
      service: api
    volumes:
      - "./src/prodik/:/prodik/src/prodik/"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:$API_PORT/ping || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5

  prometheus:
    extends:
      file: docker-compose.base.yaml
      service: prometheus

  nginx:
    extends:
      file: docker-compose.base.yaml
      service: nginx

  node-exporter:
    extends:
      file: docker-compose.base.yaml
      service: node-exporter

  grafana:
    extends:
      file: docker-compose.base.yaml
      service: grafana

  postgres:
    extends:
      file: docker-compose.base.yaml
      service: postgres
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER}", "-d", "${POSTGRES_DB}"]
      interval: 15s
      timeout: 15s
      retries: 5

volumes:
  prometheus-data:
  grafana-data:
  postgres-data:

networks:
  prodik-network:
    driver: bridge